			const line = lines[i];
			let count = 2;
			let j = i + 2;
			while (j < lines.length && lines[j] === line) {
				count++;
				j++;
			}
			result.push(`${count}× ${line}`);
			i = j;
			continue;
		}

		result.push(lines[i]);
		i++;
	}

	return result.join("\n");
}
